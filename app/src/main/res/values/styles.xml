<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- 如果加了上面的NoActionBar則windowNoTitle默認為true -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 设置window过度效果 禁用窗口的预览动画 -->
        <item name="android:windowDisablePreview">false</item>
        <item name="colorPrimaryDark">@color/colorWhite</item>
        <item name="android:textCursorDrawable">@drawable/shape_common_cursor</item>
    </style>

    <!-- 自定义loading dialog -->
    <style name="loading_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
    </style>


    <style name="textview_style">
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_width">41dp</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/colorBlack</item>
        <item name="android:textSize">30dp</item>
        <!--        <item name="android:textStyle">bold</item>-->
    </style>

    <style name="MyTheme.SplashTheme" parent="BaseAppTheme.NoActionBar">
        <!-- Android P 异性屏适配 可以达到全面屏的效果 -->
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
       <item name="android:windowBackground">@drawable/layer_splash</item>
    </style>

    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16sp</item>
        <item name="textAllCaps">true</item>

    </style>
    <style name="MyTheme.AdvertisingTheme" parent="BaseAppTheme.NoActionBar">
        <!-- Android P 异性屏适配 可以达到全面屏的效果 -->
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- NoActionBar theme. -->
    <style name="BaseAppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <!-- 自定义loading对话框的样式 -->
    <style name="CustomLoadingDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>   <!-- 是否去除边框 -->
        <item name="android:windowNoTitle">true</item>  <!-- 是否去除标题 -->
        <item name="android:windowFullscreen">true</item>   <!-- 是否全屏 -->
        <item name="android:windowIsFloating">true</item>   <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item> <!-- 背景颜色及和透明程度 -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">false</item>  <!-- 背景是否变暗 -->
    </style>
    <!-- toolbar上popUpWindow动画主题 -->
    <style name="TitleMorePopAnimationStyle">
        <item name="android:windowEnterAnimation">@anim/title_more_pop_in_anim</item>
        <item name="android:windowExitAnimation">@anim/title_more_pop_out_anim</item>
    </style>

    <style name="dialogWindowAnim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>
    </style>
    <!--自定义Dialog样式-->
    <style name="customerDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <style name="custom_dialog2" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否漂现在activity上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 去除黑色边框的关键设置项 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 屏幕背景是否变暗 -->
        <item name="android:backgroundDimAmount">0.3</item>
    </style>


    <!--居中dialog-->
    <style name="centerDialogStyle" parent="@android:style/Theme.Dialog">
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowContentOverlay">@null</item>
    <!-- 浮于Activity之上 -->
    <item name="android:windowIsFloating">true</item>
    <!-- 边框 -->
    <item name="android:windowFrame">@null</item>
    <!-- Dialog以外的区域模糊效果 -->
    <item name="android:backgroundDimEnabled">true</item>
    <!-- 无标题 -->
    <item name="android:windowNoTitle">true</item>
    <!-- 半透明 -->
    <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- BottomPopupDialog style -->
    <style name="BottomPopupDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/BottomPopupDialogAnimationStyle</item>
    </style>

    <!-- BottomPopupDialog animation style -->
    <style name="BottomPopupDialogAnimationStyle" parent="@android:style/Animation.Toast">
        <item name="android:windowEnterAnimation">@anim/bottom_popup_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/bottom_popup_dialog_exit</item>
    </style>


    <style name="addressShowDialogStyle" parent="@android:style/Animation.Toast">
        <item name="android:windowEnterAnimation">@anim/common_slide_in_from_bottom</item>
        <item name="android:windowExitAnimation">@anim/common_slide_out_to_bottom</item>
    </style>

    <!--自定义的颜色部分-->
    <color name="font_hint">#989898</color>
    <!--没有选中的样式-->
    <style name="bottom_normal_text">
        <item name="android:textColor">@color/font_hint</item>
        <item name="android:textSize">14sp</item>
    </style>
    <!--选中的样式-->
    <style name="bottom_selected_text">
        <item name="android:textColor">@color/colorBlack</item>
        <item name="android:textSize">14sp</item>
    </style>
    <style name="custom_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/addressShowDialogStyle</item>
    </style>


    <declare-styleable name="pickerview">

        <attr name="pickerview_gravity">
            <enum name="center" value="17"/>
            <enum name="left" value="3"/>
            <enum name="right" value="5"/>
        </attr>
        <attr name="pickerview_textSize" format="dimension"/>
        <attr name="pickerview_textColorOut" format="color"/>
        <attr name="pickerview_textColorCenter" format="color"/>
        <attr name="pickerview_dividerColor" format="color"/>
        <attr name="pickerview_lineSpacingMultiplier" format="float"/>
    </declare-styleable>

    <style name="pickerview_dialogAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pickerview_dialog_scale_in</item>
        <item name="android:windowExitAnimation">@anim/pickerview_dialog_scale_out</item>
    </style>

    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/diver_bg</item>
        <item name="colorControlActivated">@color/diver_bg</item>
    </style>

    <!--进度条样式-->
    <declare-styleable name="NumberProgressBar">
        <attr name="progress_unreached_color" format="color" />
        <attr name="progress_reached_color" format="color" />

        <attr name="progress_text_size" format="dimension" />
        <attr name="progress_text_color" format="color" />
    </declare-styleable>

    <style name="containerView">
        <item name="android:background">@drawable/bg_container</item>
        <item name="android:layout_marginBottom">@dimen/view_space_10</item>
        <item name="android:paddingBottom">@dimen/view_space_10</item>
        <item name="android:paddingTop">@dimen/view_space_10</item>
        <item name="android:textColor">@color/color_3</item>
    </style>
    <style name="containerView1">
        <item name="android:background">@drawable/bg_container</item>
        <item name="android:layout_marginBottom">@dimen/view_space_10</item>
        <item name="android:paddingBottom">@dimen/view_space_8</item>
        <item name="android:paddingTop">@dimen/view_space_8</item>
    </style>
    <style name="imgView">
        <item name="android:layout_width">@dimen/view_space_24</item>
        <item name="android:layout_height">@dimen/view_space_24</item>

    </style>

    <style name="imgView.left">
        <item name="android:layout_marginLeft">@dimen/view_space_10</item>
    </style>



</resources>
