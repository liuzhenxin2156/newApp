<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorWhite"
    android:orientation="vertical"
    >
    <ListView
        android:id="@+id/list_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@color/diver_bg"
        android:dividerHeight="@dimen/divider_size_0.5"
        android:footerDividersEnabled="false"
        android:headerDividersEnabled="true"
        android:listSelector="@android:color/transparent"
        android:scrollbars="none"
        />

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/view_space_8"
        android:background="@color/space_bg"
        />

    <Button
        android:id="@+id/cancel_btn"
        android:layout_width="match_parent"
        android:layout_height="@dimen/view_height_56"
        android:background="@drawable/selector_bottom_item_bg"
        android:text="取消"
        android:textColor="@color/color_3"
        android:textSize="@dimen/text_size_18"
        />

</LinearLayout>